# Backup — site público

Cópia de segurança da página **Lista pública** antes de remover o botão no site.

- `lista.html` — versão original que listava todos os associados.

O site público passou a usar **somente consulta individual por CPF** (`consulta.html`).
Se alguém acessar `/lista.html`, é redirecionado para a consulta.
